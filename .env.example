# Set a unique admin password before using live rooms.
ADMIN_PASSWORD=replace-with-a-long-random-password

# Server-only Supabase configuration. Never use NEXT_PUBLIC_ for the secret key.
SUPABASE_URL=https://slppvgumlwuuclqgnevt.supabase.co
# NEXT_PUBLIC_SUPABASE_URL is also supported if SUPABASE_URL is not set.
SUPABASE_SECRET_KEY=
# Alternatively, older projects can use SUPABASE_SERVICE_ROLE_KEY.
